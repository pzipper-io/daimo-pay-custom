{
  "name": "daimo-pay-custom",
  "private": true,
  "workspaces": [
    "packages/*",
    "examples/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "test": "npm run test --workspaces",
    "lint": "npm run lint --workspaces",
    "dev": "npm run dev --workspace=@daimo/pay"
  },
  "devDependencies": {
    "typescript": "^5.5.2"
  },
  "dependencies": {
    "@worldcoin/minikit-js": "^1.9.6"
  }
}
